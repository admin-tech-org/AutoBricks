{
  "name": "bricks-cdp-visual-generator",
  "version": "0.1.0",
  "private": true,
  "description": "Render a real website via CDP/Playwright, capture screenshot + DOM + CSS + layout, merge into Page IR, and generate clean importable Bricks Builder JSON.",
  "workspaces": [
    "packages/*",
    "apps/*",
    "workers"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.json && npm run build -w apps/dashboard",
    "clean": "tsc -b tsconfig.json --clean",
    "api": "node apps/api/dist/server.js",
    "generate": "node apps/api/dist/cli.js",
    "browsers": "playwright install chromium"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=18"
  }
}
