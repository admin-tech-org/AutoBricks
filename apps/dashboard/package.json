{
  "name": "@bricks-cdp/dashboard",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild app.tsx --bundle --outfile=public/bundle.js --jsx=automatic --define:process.env.NODE_ENV=\"'production'\" --minify"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0"
  }
}
